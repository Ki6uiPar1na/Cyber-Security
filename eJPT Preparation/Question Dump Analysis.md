**All Question**
1. What is the IP address of the host running WordPress? 
	#Ans1: `192.168.100.50`
2. What is the IP address of the host running SAMBA 139 445
	
3. How many hosts on the DMZ network are running a database server?
	`2`
4. What version of MySQL is running on the system hosting a Drupal site? 
	#Ans4`MySQL 5.5.5`
5. What version of Windows is running on the host running WordPress?
6. What is the name of the user account that published a blog post on the *Drupal* site? 
7. What is the email of the admin user on the Drupal site? 
8. What is the name of active theme on the WordPress site? 
9. What is the IP address of the host vulnerable to an SSH brute-force attack?
10. What is the IP address of the FTP server that contain a file updates.txt
11. What type of vulnerability can be exploited on the Drupal size? 
12. What type of vulnerability can be exploited to gain access to WINSERVER-03?
13. What type of vulnerability can be exploited on the WordPress site to obtain a reverse shell? 
14. How many hosts exist within the internal network that cannot be access through the DMZ network?
15. Which one of the following meterpreter command can be used to add a network route? 
16. One of the Linux server in the internal network is running a vulnerable service. What port is the vulnerable service running on? 
17. What is the password of the user account "dbadmin" on the Linux server hosting Drupal?
18. A target system has a user account called "lawrence". What is the password for this account? 
19. What is the password for the user "mike" on WINSERVER-01?
20. What is the name of vulnerable web app running on the Linux server in the internal network? 
21. What is the CVSS V3.x rating for the Drupalgeddon2 vulnerability?
22. What file can be used to identify the version of the Drupal running on a webserver?
23. What is the password for the "admin" user account on WordPress?
24. How many pluggins are installed on the WordPress site? 
25. What versino of WordPress is running on WINSERVER-01?
26. What WordPress file stores the database configuration?
27. What is the root password of the MySQL database on the server running Drupal?
28. Excluding the guest account, how many user account are present on WINSERVER-01?
29. What host in the DMZ network is running a web server with WebDAV enabled?
30. What host on the network contain a user account callled "lawrence"?
31. What is the hashing algorithm used to hash user account passwords on both Linux servers?